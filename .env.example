# Never commit .env.local. Rotate any credentials that have been shared in screenshots.
GITHUB_TOKEN=github_pat_your_token_here
GITHUB_OWNER=MuhammadAfridi1
GITHUB_REPO=simple-research
ADMIN_KEY=create-a-long-random-local-key

# Leave unset so the private desk only accepts localhost requests.
# ALLOW_REMOTE_ADMIN=false

# Optional: reserved for a future lightweight MongoDB index.
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/research_archive